## 1.0.0

* Initial release
* Provides `RiveViewModel` abstract class with lifecycle management
* Includes `dispose()` method for resource cleanup
* Includes `isDisposed` getter for disposal state tracking
